import { Form } from "react-router-dom";

// In the future, instead of having a separate folder page it will just
// edit what content is being shown on the notes page
export default function Folder() {
    return (
        <div className="page green-background">
            <p>Placeholder Folder</p>
        </div>
    );
}
